import 'package:flutter/material.dart';

class ScreenWebView extends StatelessWidget {
  const ScreenWebView({super.key, required this.websiteURL});
  final String websiteURL;
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
